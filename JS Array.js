//array is consider as a object in javascript.
let seven = []; //Initialize the empty array
let selectedColors = [3, 'Arcade']; //Added new value in a function for output
selectedColors[2] = 'Mihir'; //Add new value in same function because of dynamic size

/* console.log(selectedColors); */
//It have multiple properties to use in javascript one of them is-
console.log(selectedColors.length); //length is use to check length of an array
