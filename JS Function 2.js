//Using function ability creating a basic maths function for usecase
//Added function named maths and used for testing basic equation
function maths(number) {
    return number * number - 33;
}

//declared with argument and input amount to do calculations
//and show the output through window alert at first
/* let number = maths(12);
window.alert(number); */ //first way to show the output

//We can directly show the output through window alert with argument function
window.alert(maths(12)); 