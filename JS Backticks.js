//Template Literals - Backticks(`)
//It used to replace text/item/anything from first one 
/* first it will give outcome replace text and addition of original text like
(${obj/item}) */
//with help of these we don't have to make addition each time
x = 900;
document.write(`I have just changed text replace with normal ${x} `);